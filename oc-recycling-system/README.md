# OC Recycling System

React + Vite + Tailwind + React Router app for managing a local original-character archive.

## Run locally

1. `npm install`
2. `npm run dev`

The app persists data in browser `localStorage` under the key `oc-recycling-system-v1`.
